version: '3.8'
services:
  flask:
    build: .
    volumes:
      - ./web:/usr/src/web
      -  ./media:/home/media
    expose:
      - "5000"

  nginx:
    image: nginx:latest
    ports:
      - "8088:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - flask
